// FULL UPDATED LANDING PAGE (keeps your original structure + adds conversion improvements)

import { useState } from "react";
import { motion } from "framer-motion";
import { Link } from "wouter";
import {
  ArrowRight,
  CheckCircle2,
  Zap,
  Shield,
  BarChart,
  Server,
  Mail,
  Activity,
  Send,
  Users,
  Filter,
  FlaskConical,
  BarChart3,
  TrendingUp,
  Globe,
  Lock,
  Cpu,
  Tag,
  ChevronDown,
  ChevronUp
} from "lucide-react";

import { PublicLayout } from "@/components/layout/PublicLayout";
import { Button, Card, CardContent } from "@/components/ui/core";
import { cn } from "@/lib/utils";

// KEEP ALL YOUR FEATURES (unchanged)
const ALL_FEATURES = [/* keep your original ALL_FEATURES here exactly */];

function FeatureCard({ feature }) {
  const [expanded, setExpanded] = useState(false);
  const Icon = feature.icon;
  const preview = feature.items.slice(0, 4);
  const rest = feature.items.slice(4);

  return (
    <Card className="hover:shadow-xl transition-shadow h-full">
      <CardContent className="p-8 flex flex-col h-full">
        <div className="w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center text-primary mb-5">
          <Icon className="w-6 h-6" />
        </div>

        <h3 className="text-xl font-bold mb-2">{feature.title}</h3>
        <p className="text-muted-foreground text-sm mb-5">{feature.desc}</p>

        <ul className="space-y-2 mb-4">
          {preview.map((item) => (
            <li key={item} className="flex gap-2 text-sm">
              <CheckCircle2 className="w-4 text-primary" />
              {item}
            </li>
          ))}
          {expanded && rest.map((item) => (
            <li key={item} className="flex gap-2 text-sm">
              <CheckCircle2 className="w-4 text-primary" />
              {item}
            </li>
          ))}
        </ul>

        {rest.length > 0 && (
          <button onClick={() => setExpanded(!expanded)} className="text-xs text-primary">
            {expanded ? "Show less" : `+${rest.length} more`}
          </button>
        )}
      </CardContent>
    </Card>
  );
}

export function Landing() {

  const plans = [
    {
      name: "Free",
      price: "0",
      desc: "Use your own SMTP",
    },
    {
      name: "Pro",
      price: "21",
      desc: "Use your SMTP + advanced features",
    },
    {
      name: "Premium",
      price: "49",
      desc: "Best for automation & scale",
    }
  ];

  return (
    <PublicLayout>

      {/* HERO (UPDATED POSITIONING) */}
      <section className="pt-20 pb-32 text-center">
        <div className="max-w-4xl mx-auto px-4">

          <h1 className="text-5xl font-extrabold mb-6">
            Run your own email infrastructure —
            <br />
            <span className="text-primary">without deliverability headaches</span>
          </h1>

          <p className="text-lg text-muted-foreground mb-8">
            Self-hosted email platform with optional managed delivery and optimized inbox performance.
          </p>

          <div className="flex justify-center gap-4">
            <Link href="/register">
              <Button size="lg">Start Free <ArrowRight className="ml-2"/></Button>
            </Link>
            <Button size="lg" variant="outline">Get Managed Setup</Button>
          </div>

        </div>
      </section>

      {/* DIY VS MANAGED (NEW SECTION) */}
      <section className="py-20 bg-muted/20">
        <div className="max-w-6xl mx-auto grid md:grid-cols-2 gap-8 px-4">

          <Card>
            <CardContent className="p-8">
              <h3 className="font-bold mb-3">Use Your Own SMTP</h3>
              <ul className="text-sm space-y-2">
                <li>SES / SendGrid support</li>
                <li>Lower cost</li>
                <li>Basic tracking</li>
              </ul>
            </CardContent>
          </Card>

          <Card className="border-primary">
            <CardContent className="p-8">
              <h3 className="font-bold mb-3">Managed Delivery</h3>
              <ul className="text-sm space-y-2">
                <li>Better inbox placement</li>
                <li>Dedicated IP</li>
                <li>Full monitoring</li>
              </ul>
              <Button className="mt-4 w-full">Get Managed Setup</Button>
            </CardContent>
          </Card>

        </div>
      </section>

      {/* FEATURES (KEEP FULL GRID) */}
      <section className="py-24">
        <div className="max-w-7xl mx-auto px-4">

          <h2 className="text-4xl text-center mb-16 font-bold">
            Complete Feature Set
          </h2>

          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
            {ALL_FEATURES.map((f) => (
              <FeatureCard key={f.title} feature={f} />
            ))}
          </div>

        </div>
      </section>

      {/* EMAIL HEALTH (NEW) */}
      <section className="py-20 bg-muted/20 text-center">
        <h2 className="text-3xl font-bold mb-6">Email Health</h2>
        <div className="flex justify-center gap-6">
          <Card><CardContent className="p-4">SPF ✅</CardContent></Card>
          <Card><CardContent className="p-4">DKIM ⚠️</CardContent></Card>
          <Card><CardContent className="p-4">DMARC ❌</CardContent></Card>
        </div>
      </section>

      {/* PRICING (CLARIFIED) */}
      <section className="py-20">
        <div className="max-w-6xl mx-auto px-4">

          <h2 className="text-3xl text-center mb-12 font-bold">Pricing</h2>

          <div className="grid md:grid-cols-3 gap-6">
            {plans.map((p) => (
              <Card key={p.name}>
                <CardContent className="p-6">
                  <h3 className="font-bold">{p.name}</h3>
                  <p className="text-3xl">${p.price}</p>
                  <p className="text-sm text-muted-foreground">{p.desc}</p>
                  <Button className="w-full mt-4">Get Started</Button>
                </CardContent>
              </Card>
            ))}
          </div>

        </div>
      </section>

      {/* SMTP (UPGRADED POSITIONING) */}
      <section className="py-20 bg-muted/20 text-center">
        <h2 className="text-3xl font-bold mb-6">
          Fix deliverability. Stop landing in spam.
        </h2>
        <p className="text-muted-foreground mb-6">
          Optimized delivery with warmup, reputation control, and monitoring.
        </p>
        <Button size="lg">Get Managed Setup</Button>
      </section>

      {/* FINAL CTA */}
      <section className="py-20 text-center">
        <h2 className="text-4xl font-bold mb-6">
          Start free or get it fully set up
        </h2>
        <div className="flex justify-center gap-4">
          <Link href="/register"><Button>Start Free</Button></Link>
          <Button variant="outline">Get Setup Done</Button>
        </div>
      </section>

    </PublicLayout>
  );
}
